# دليل سريع لبناء APK

## ⚠️ Java غير مثبت!

لبناء APK، يجب تثبيت Java Development Kit (JDK) 17 أولاً.

---

## 📥 الحل السريع - حمّل Java:

### الطريقة 1: تحميل يدوي (الأسرع)

1. **حمّل Java 17:**
   - https://download.oracle.com/java/17/latest/jdk-17_windows-x64_bin.exe
   
2. **ثبّت Java:**
   - شغّل الملف المحمّل
   - اتبع التعليمات (Next, Next, Install)
   
3. **أعد فتح PowerShell** وشغّل:
   ```powershell
   cd android
   .\gradlew.bat assembleDebug
   ```

4. **APK سيكون في:**
   ```
   android\app\build\outputs\apk\debug\app-debug.apk
   ```

---

### الطريقة 2: استخدم Android Studio (الأسهل)

1. **حمّل Android Studio:**
   - https://developer.android.com/studio
   - يشمل Java تلقائياً!

2. **بعد التثبيت:**
   ```powershell
   npx cap open android
   ```

3. **في Android Studio:**
   - Build → Build Bundle(s) / APK(s) → Build APK(s)
   - انتظر 2-3 دقائق
   - اضغط "locate" في الإشعار

---

## 🎯 طريقة بديلة - Expo EAS (بدون Java!)

إذا لم ترد تثبيت Java:

```powershell
# 1. ثبّت EAS CLI
npm install -g eas-cli

# 2. حوّل لـ Expo
npx expo init --template blank

# 3. ابني في السحابة
eas build --platform android --profile preview
```

---

## ✅ بعد الحصول على APK:

### نقل لهاتف الأندرويد:

**عبر USB:**
```powershell
# وصّل الهاتف بالكمبيوتر
adb install petrofac-lubrication.apk
```

**عبر البريد/WhatsApp:**
- أرسل الملف لنفسك
- حمّله على الهاتف
- افتح الملف وثبّت

**عبر Google Drive/Dropbox:**
- ارفع APK للسحابة
- حمّله من الهاتف
- ثبّت

---

## 📋 الملخص:

| الطريقة | الوقت | الصعوبة |
|---------|-------|---------|
| Java + Gradle | 10 دقائق | متوسطة |
| Android Studio | 30 دقيقة | سهلة |
| EAS Cloud | 5 دقائق + انتظار | سهلة جداً |

---

## 🔧 إذا واجهت مشاكل:

```powershell
# نظّف وأعد البناء
cd android
.\gradlew.bat clean
.\gradlew.bat assembleDebug
```

---

## 💡 ملاحظة مهمة:

التطبيق جاهز 100%! فقط يحتاج Java لتحويله إلى APK.

**معلومات التطبيق:**
- الاسم: Petrofac Lubrication
- المعدات: 827
- الواجهة: احترافية داكنة
- الحجم المتوقع: 5-8 MB

---

## 📞 للمساعدة:

اتبع أي طريقة من الطرق أعلاه وستحصل على APK جاهز للتثبيت! 🎉
